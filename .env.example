# Copy to .env and fill in. Never commit .env.

# Required for AI features (free tier): https://aistudio.google.com
CATO_GEMINI_KEY=

# Optional: override API base URL used by the Next.js frontend
# BACKEND_URL=http://127.0.0.1:8000

# Optional: where cato_mind.db lives (Electron sets this automatically when packaged)
# VOLTAIRE_DATA_DIR=
